<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.preranasingh.contacts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Contacts">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CreateNewContact"
            android:label="@string/CreateNewContact" />
        <activity
            android:name=".ContactsList"
            android:label="Contacts List" />

        <uses-feature android:name="android.hardware.camera" />

        <activity android:name=".ListItemUI" />
        <activity android:name=".EditContactActvity"></activity>
    </application>

</manifest>